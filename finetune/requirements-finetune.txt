# Only needed for finetune/train_lora.py and finetune/merge_and_export.py
# Kept separate from the main requirements.txt since these are heavy
# (torch + CUDA) and not needed for the RAG/backtest/paper-trading workflow.

torch>=2.2.0
transformers>=4.44.0
peft>=0.12.0
bitsandbytes>=0.43.0
accelerate>=0.33.0
datasets>=2.20.0
trl>=0.9.6
