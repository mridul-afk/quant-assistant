anthropic>=0.40.0
chromadb>=0.5.0
sentence-transformers>=3.0.0
pypdf>=4.2.0
yfinance>=0.2.40
pandas>=2.2.0
numpy>=1.26.0
matplotlib>=3.8.0
python-dotenv>=1.0.0
tqdm>=4.66.0

# Optional: only needed if LLM_BACKEND=local in .env
# On Windows with an NVIDIA GPU (matches the Jarvis setup), install with CUDA support instead:
#   pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu121
# llama-cpp-python>=0.3.0
